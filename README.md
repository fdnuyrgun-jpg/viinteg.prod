
# VIntegCorp - Корпоративный портал

Современный и минималистичный корпоративный портал для управления внутренними процессами компании, обмена знаниями и командного взаимодействия.

## Технологический стек

- **Frontend**: React 19, TypeScript, Tailwind CSS.
- **Backend**: Serverless Functions (Node.js).
- **Database**: PostgreSQL (Neon).

---

## 🚀 Инструкция по деплою (Production)

### 1. Подготовка базы данных (Neon)
1. Создайте проект на [neon.tech](https://neon.tech).
2. Скопируйте **Connection String** (Pooled connection рекомендуется).
3. Она выглядит примерно так: `postgres://user:pass@ep-xyz.us-east-1.aws.neon.tech/neondb?sslmode=require`.

### 2. Настройка проекта
1. Создайте файл `.env.local` в корне проекта:
   ```env
   DATABASE_URL=ваш_neon_connection_string
   JWT_SECRET=придумайте_сложный_пароль_минимум_32_символа
   ```
2. **Инициализация БД**:
   Запустите команду, чтобы создать таблицы и первого администратора в удаленной базе:
   ```bash
   npm run db:setup
   ```
   *Если команда прошла успешно, вы увидите сообщение "Администратор создан".*

### 3. Деплой на Vercel
1. Установите Vercel CLI (`npm i -g vercel`) или используйте веб-интерфейс GitHub интеграции.
2. Импортируйте проект.
3. В настройках **Environment Variables** добавьте:
   - `DATABASE_URL`: (тот же, что в п.1)
   - `JWT_SECRET`: (тот же, что в п.2)
4. Нажмите **Deploy**.

---

## 🔑 Доступ после деплоя

После успешного деплоя и выполнения `npm run db:setup`, используйте следующие данные для первого входа:

- **Email**: `admin@corppulse.com`
- **Пароль**: `admin123`

> **Важно!** Сразу после входа зайдите в профиль и смените пароль администратора.

---

## Локальная разработка

```bash
# Установка зависимостей
npm install

# Запуск локального сервера
npm run dev
```

Приложение будет доступно по адресу `http://localhost:3000`.
